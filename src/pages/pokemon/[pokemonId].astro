---
export function getStaticPaths() {
  const paths = [];
  for (let i = 1; i <= 1025; i++) {
    paths.push({ params: { pokemonId: i.toString() } });
  }

  return paths;
}
---

<div>Soy la ruta pokemonID</div>
